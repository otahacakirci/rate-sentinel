# Optional. When empty, the client derives the SockJS endpoint from window.location.origin.
VITE_WEBSOCKET_URL=

# Optional. When empty, control-plane requests use same-origin relative paths.
VITE_API_BASE_URL=
